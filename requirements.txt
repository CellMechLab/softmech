# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
h5py>=3.0.0
lmfit>=1.0.0              # Non-linear least-squares fitting for curve analysis

# UI dependencies
PySide6>=6.2.0
pyqtgraph>=0.13.0        # Primary plotting library for Designer/Analyzer UIs (10-50x faster than matplotlib, GPU-accelerated)
matplotlib>=3.4.0         # Optional: for report export and publication-ready figures

# CLI dependency
Click>=8.0.0

# Development
pytest>=7.0.0
pytest-cov>=3.0.0
magicgui>=0.7.0          # Auto-generate parameter UIs from type hints
